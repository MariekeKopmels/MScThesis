source SkinVenv/bin/activate 
deactivate

wandb sweep sweep.yaml

Current env
pip install --upgrade pip
pip install opencv-python
pip install torch
pip install torchvision
pip install matplotlibpip install torch

pip install torchmetrics
pip install wandb
pip install tqdm

wandb login

Get interpreter path:
python3
import sys
print(sys.executable)
/Users/mariekekopmels/Desktop/Uni/MScThesis/Code/Envs/SkinVenv/bin/python3

Als je wil printen met cv2.imshow(), moet een Tensor.float32 terug geconvert worden met .astype(np.uint8)

cv2.imwrite takes height, width, channels

2 ipv 1 Black/white channel
        # ret,gt_skin = cv2.threshold(gt,127,255,cv2.THRESH_BINARY)
        # ret,gt_background = cv2.threshold(gt,0,126,cv2.THRESH_BINARY)
        # gt = np.stack((gt_skin, gt_background), axis=-1)